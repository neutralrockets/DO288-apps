#!/bin/bash

echo "Performing the S2I build..."

/usr/libexec/s2i/assemble

rc=$?

if [ $rc -eq 0 ]; then
    echo "Recording successful build in the lifecycle management system..."
else
    echo "Not calling the lifecycle management system: S2I build failed!"
fi
exit $rc
